name: ilm_ai_flutter
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Navigation
  go_router: ^14.6.2

  # Networking
  dio: ^5.7.0

  # Models / serialization
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

  # Local storage
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.3

  # Graphics / icons
  flutter_svg: ^2.0.16

  # Animation
  flutter_animate: ^4.5.2
  shimmer: ^3.0.0

  # NOTE: media/picker/auth/push packages below are deferred until the phase
  # that actually implements them (Phase 2 files, Phase 1 Google OAuth,
  # Phase 3 voice/avatar, Phase 4 push cutover) -- added one at a time to
  # isolate Android-toolchain (AGP 9 / Kotlin 2.3) compatibility issues per
  # package rather than debugging them all at once.
  just_audio: ^0.9.42
  record: ^7.1.1
  flutter_tts: ^4.2.0
  image_picker: ^1.1.2
  # file_picker: ^11.0.2 -- incompatible with this AGP9/Kotlin toolchain
  # combo right now: 8.x hardcodes compileSdk 34 (breaks flutter_plugin_
  # android_lifecycle's >=36 requirement), 11.x needs android.builtInKotlin
  # =true which conflicts with android.newDsl=false (required by this
  # Flutter version). Revisit when either side ships a fix; for now
  # KnowledgeBaseScreen uses the text-paste upload path only.
  image: ^4.3.0
  flutter_web_auth_2: ^4.1.0
  # Push notifications: code is wired (see core/notifications/push_service.dart)
  # but stays inert until the user adds real Firebase project files --
  # android/app/google-services.json and ios/Runner/GoogleService-Info.plist.
  # Firebase.initializeApp() is wrapped in try/catch so a missing config
  # disables push registration silently instead of crashing the app.
  firebase_messaging: ^15.1.6
  flutter_local_notifications: ^18.0.1
  firebase_core: ^3.10.0

  # Device/platform info
  device_info_plus: ^11.2.0
  path_provider: ^2.1.5

  # URL launching (Telegram deep link, etc.)
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Code generation
  build_runner: ^2.4.14
  riverpod_generator: ^2.6.3
  freezed: ^2.5.7
  json_serializable: ^6.9.0

  # Icon / splash generation (run once via dart run <package>:main)
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.4

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/icon.png"
  adaptive_icon_background: "#1a1a3e"
  adaptive_icon_foreground: "assets/icons/adaptive-icon.png"

flutter_native_splash:
  color: "#ffffff"
  image: "assets/icons/splash.png"
  android_12:
    color: "#ffffff"
    image: "assets/icons/splash.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/data/colleges-us.json
    - assets/data/colleges-eu.json
    - assets/data/colleges-curated.json
    - assets/icons/icon.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
